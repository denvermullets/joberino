%div.container
  %br
  %p.job-count
    = "#{all_jobs.count} jobs displayed out of #{job_count} jobs posted"
  - if all_jobs.any?
    - all_jobs.each do |single_job|
      = render partial: 'job_listings/job_card', locals: { job: single_job }